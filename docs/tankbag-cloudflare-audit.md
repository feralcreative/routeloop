# tankbag.app Cloudflare/GCP Audit Report

## TASK 1—Web Analytics current state

1. **Yes**, tankbag.app is listed as a site in Web Analytics (Cloudflare dashboard → Web Analytics → Sites list), created 22 days ago.
2. **Setup type: Automatic**, but the RUM toggle under "Manage site" is currently set to **"Enable, excluding visitor data in the EU"**—not plain "Enable" and not "Manual." This means the JS snippet _is_ auto-injected, but only for non-EU visitors. That's a likely partial explanation for why your HTML fetch showed no beacon: if that fetch was made from an EU exit node/VPN, or Cloudflare geo-detected it as EU, no snippet would be injected for that particular response. A site token wasn't visible anywhere in the UI—Cloudflare only displays the token/snippet when the mode is switched to "Enable with JS Snippet installation" (manual), which the account is not currently using. I did not save that toggle change; I only viewed it and clicked Cancel.
3. **Not empty.** Last 24h shows Visits: 9, Page views: 17 (dashboard: `dash.cloudflare.com/.../web-analytics/overview?siteTag~in=8899fc01688e48d790d7abb535f719d8`). So despite your curl showing no beacon, real beacon data is arriving for at least some visitors.
4. **Cache-Control check**: I was not able to directly inspect the live response headers from this session (no shell/curl access), so I can't independently confirm whether `Cache-Control: public, no-transform` is being sent by your Node/Hono app. Flagging this as unverified—you'll need to check this server-side (e.g., `curl -I https://tankbag.app`) or via a network panel. Cloudflare's own docs (quoted in Task 2) confirm this exact header is a documented cause of injection failure, which combined with the EU-exclusion setting gives two plausible, non-exclusive explanations.

## TASK 2—Exact data collected (all claims sourced from developers.cloudflare.com)

1. **Dimensions**, per the live filter picker on your dashboard and confirmed by docs at `https://developers.cloudflare.com/web-analytics/data-metrics/dimensions/`: Country, Host, Path, Referer, Device type, Browser, Operating system, Navigation type, plus Site and Exclude Bots (account-level filters, not visitor data). Your description ("path, referring site, country, browser/device type") is basically right but incomplete—it's missing **Host**, **Operating system**, and **Navigation type** as distinct recorded/filterable dimensions.
2. **Yes—full path is recorded, including your slugs.** I saw this directly on your live dashboard: the "Top URLs" breakdown for tankbag.app literally lists a row reading `tankbag.app/m/9qOpnCnXfQWS6lbqojeO1..` (truncated in the UI). So any visit to an unlisted `/m/:slug` trip will appear as its own row in Web Analytics, visible to anyone with dashboard access. The docs confirm Path is "the links within your site referring visits to a page" with no path-masking. One caveat: Cloudflare does **not** log query strings ("Cloudflare Web Analytics do not log query strings to avoid collecting potentially sensitive data"—`https://developers.cloudflare.com/web-analytics/faq/`), but the path itself, including your slug segment, is not protected by this.
3. **Yes, via "Rules"** (`https://developers.cloudflare.com/web-analytics/configuration-options/rules/`): rules can turn tracking on/off per hostname+path. However, per limits (`https://developers.cloudflare.com/web-analytics/limits/`), **the Free plan has a rules limit of 0**—rules require Pro or higher. This matches what I saw live: your "Advanced Options → Web Analytics Rules" panel is locked behind "Upgrade plan," and your zone is on the Free plan. So today, on this plan, there is no way to exclude `/m/:slug` from being recorded.
4. **I could not find this exact quote within the developers.cloudflare.com docs I have access to.** The closest official statement I found is on the "About" page: _"Cloudflare Web Analytics does not collect or use your visitors' personal data"_ (`https://developers.cloudflare.com/web-analytics/about/`). The specific "no cookies, no localStorage, no fingerprinting" phrasing you're likely thinking of lives on Cloudflare's main marketing site (`www.cloudflare.com/web-analytics/`), which is outside the domains approved for this session. If you want, I can get your go-ahead to visit that domain and pull the exact sentence.
5. **Confirmed.** Docs: _"Visits - A page view that originated from a different website or direct link. Cloudflare checks where the HTTP referer does not match the hostname."_ (`https://developers.cloudflare.com/web-analytics/data-metrics/high-level-metrics/`). So yes, it's Referer-based, not cookie-based.
6. **Beacon host:** `https://static.cloudflareinsights.com/beacon.min.js`. Data is POSTed to `https://tankbag.app/cdn-cgi/rum` (same-origin, since you're proxied through Cloudflare) or to `cloudflareinsights.com/cdn-cgi/rum` for non-proxied/manual setups. Source: `https://developers.cloudflare.com/web-analytics/data-metrics/data-origin-and-collection/`. Docs don't describe it setting any cookie or persistent client-side storage—it operates purely off the Performance API and `sendBeacon`.

## TASK 3—Cloudflare Access

**No Access application or policy currently covers tankbag.app.** I checked Zero Trust → Access → Applications (only 3 apps exist: "RouteLoop Login," "RouteLoop Login (stage)," and "Print (print.ezzat.com)") and both Reusable and Legacy Policies tabs (1 reusable policy + 2 legacy policies, all tied to RouteLoop apps). Searching "tankbag" in the Applications list returned zero records. So there is nothing to remove for tankbag.app specifically—whatever redundant Access gate you remember must have already been deleted, was never created under this account, or exists under a different Cloudflare account/team than this one (`Feral Creative`, account ID `30aa05fc7822ef63635046e5c7771a9a`). Seats: the Zero Trust overview shows "Users: 2 of 50 available" account-wide, but since no tankbag app/policy exists, there's no seat count specifically billed to it. **I made no changes**—please double check you're looking at the right Cloudflare account/team if you expected to find one here.

## TASK 4—GCP API quotas: blocked, needs your action

I opened `console.cloud.google.com/apis/dashboard` and it redirected straight to a Google sign-in page—there's no active Google session in this browser. Per my security rules I can't log in or enter credentials on your behalf. **Please sign in to the Google account tied to the Tankbag project in this browser tab**, and let me know once you're in—I'll then pull the Maps JavaScript API / Places API / Routes API quota and usage figures, check the billing budget alert, and come back with proposed daily caps (not applying anything until you confirm).

## Flags / unresolved items

- The actual `Cache-Control` response header from tankbag.app (Task 1.4)—needs a server-side check or your go-ahead to fetch it.
- The exact "no cookies/localStorage/fingerprinting" sentence with a cloudflare.com URL (Task 2.4)—needs your go-ahead to expand browsing to `www.cloudflare.com`.
- GCP quota/usage/budget data (Task 4)—needs you to sign in to Google Cloud Console first.
